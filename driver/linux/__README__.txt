This is a "rules" definition for udev. It gives all members of the group "plugdev" access to CoCo devices.
Place it in you "rules.d" folder, which is usually "/etc/udev/rules.d", and re-plug the device. That's it. :)
